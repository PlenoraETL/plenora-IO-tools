# Matrice di tracciabilità assurance

Stato: `Soddisfatto`, `Parziale`, `Aperto`. La matrice descrive evidenza nel
repository, non compliance DO-178C.

| ID | Hazard controllato | Requisito | Evidenza | Stato |
|---|---|---|---|---|
| PLN-ASR-001 | H-04 arresto del processo | Nessun `unsafe` nei crate di libreria del workspace; confini FFI dipendenti governati separatamente | `#![forbid(unsafe_code)]`; gate CI `-D unsafe-code` sui target `lib`; fork `gdal 0.17.1` e `dxf 0.6.1` bloccati da checksum/revisione upstream, hash dell'albero e delta registrato | Parziale: i due fork sono inventariati, le altre dipendenze transitive/native non sono qualificate |
| PLN-ASR-002 | H-04 arresto del processo | Nessuna primitiva esplicita di panic nei target `lib`; una terminazione anomala dei parser in background non è un falso EOF | Gate Clippy `unwrap/expect/panic/unreachable/todo/unimplemented`; `spawn_batch_reader` intercetta panic/disconnessione e restituisce `PlenoraIoError` | Soddisfatto |
| PLN-ASR-003 | H-01 corruzione geometrica | WKB/EWKB troncati, dimensionalmente incoerenti o oltre limite sono rifiutati | Unico codec autoritativo `plenora-io-model::wkb_lossless`, esposto da `wkb`; decode/inspect/encode dei 15 tipi geometrici concreti R3.1, con `Unknown` solo dichiarativo; visitor confrontato col decoder; test figli canonici, Z/M/SRID e rifiuto di linearizzazioni; corpus deterministico con limiti, trailing byte e conteggi avversari | Soddisfatto nel perimetro del codec lossless; capability dei singoli formati restano più ristrette |
| PLN-ASR-004 | H-03 esaurimento risorse | Ogni operazione applica limiti caller-controlled prima di crescita non bounded | `Limits`, `WkbLimits`, wrapper reader/writer e test limite; dimensionatore adattivo condiviso; reader KML/XLSX con spool bounded e DXF con buffer da 64 MiB più spill su file; writer DXF limitato durante la serializzazione; campagne A/B su allocazioni, byte e RSS | Parziale |
| PLN-ASR-005 | H-02 sovrascrittura output | Publish atomico, same-filesystem e no-clobber autorevole | ADR-IO 2; test cross-filesystem, TOCTOU, crash e recovery; job macOS su `renameatx_np(RENAME_EXCL)` | Parziale: soddisfatto Linux/Android, Windows e macOS; directory publish non disponibile sui BSD |
| PLN-ASR-006 | H-05 falsa conferma di durabilità | La durabilità non verificabile è riportata nell'esito | `PublishOutcome`; Windows restituisce `PublishedButDurabilityUnconfirmed` | Parziale: matrice filesystem aperta |
| PLN-ASR-007 | H-01 perdita dati silenziosa | Conversioni e coercion sono fail-closed o rendicontate | ADR-IO 3 e 5; capability gate con set dei tipi geometrici; `FidelityAssessment` e `LossReport`; `convert` separa `read_loss`/`write_loss` e pubblica `conversion_fidelity`; errori dei worker preservati per variante; parser dei metadati geometrici chiuso; default legacy applicato prima del parsing e distinzione assente/`unknown` conforme alla direzione R3.4; nomi wire conformi a R3.1; geometrie GeoJSON vuote rifiutate prima dell'emissione | Parziale per driver |
| PLN-ASR-008 | H-06 interpretazione CRS errata | CRS assente, irrisolto e axis order non sono confusi | ADR-IO 4; `CrsResolution`, `RawCrs`, test axis order; chiavi schema `crs_id`, `crs_resolution`, `crs_definition`, `crs_definition_format`, `axis_order`; SHP/GPKG/DXF fail-closed su resolved senza ID | Parziale per resolver |
| PLN-ASR-009 | H-01/H-02 stato parziale | Lifecycle writer invalida dopo errore e pubblica soltanto a `finish` | ADR-IO 1; `StagedFile` terminale centralizza staging/limite/publish dei file singoli; test su doppio publish, limite, drop, poison, abort, concorrenza e crash FileGDB | Soddisfatto nel profilo corrente |
| PLN-ASR-010 | H-07 baseline non riproducibile | Toolchain e grafo dipendenze sono fissati e sottoposti ad audit | `rust-toolchain.toml` 1.92.0; dipendenze dirette con pin esatti e punto unico; GitHub Actions Node 24 fissate a commit SHA e CIA del rinnovo; gate manifest/workflow; `Cargo.lock`, `--locked`, `cargo audit --deny warnings`; eccezioni motivate in `DEPENDENCY_EXCEPTIONS.md` | Parziale: immagini runner e pacchetti nativi installati da `apt` restano mobili |
| PLN-ASR-011 | H-08 verifica insufficiente | Regressioni hanno test deterministici e coverage misurata | CI candidata 1.0 RC1 `796a1f9` (`30617658910`) verde su Linux/Windows/macOS; artifact LCOV `8788035291` con digest SHA-256 API registrato e gate librerie 80% superato; FileGDB feature-on, fork governati, fuzz e test worker | Parziale: branch coverage e MC/DC assenti |
| PLN-ASR-012 | H-09 modifica non analizzata | Ogni modifica registra impatto ed evidenza | Template PR, questa matrice e change record in `docs/assurance/CHANGE_IMPACT_*.md`, inclusa baseline/veto/risultati della terza campagna prestazionale | Parziale: revisione indipendente non disponibile |
| PLN-ASR-013 | H-01 valore inventato | Ogni fallback semantico non-panicking è censito e non può crescere senza review | `FALLBACK_REGISTER.md`; gate a 92 occorrenze workspace, di cui 45 nel componente distribuibile, tramite `check_assurance_fallbacks.sh`; regressioni CSV/CRS/FileGDB, interi, GeoJSON/KML vuoti, diagnostica XML, XLSX XY e spool DXF | Parziale: registro semantico, non prova formale |
| PLN-ASR-014 | H-01/H-07 identità di confine ambigua | Package e tipi pubblici locali non collidono con componenti Plenora distinti | package `plenora-io-model`; errore `PlenoraIoError`; gate `check_public_identity.py`; confronto con `plenora-data-tools` | Soddisfatto nel perimetro corrente; crate condiviso subordinato alla ratifica di §15.3 |
| PLN-ASR-015 | H-01/H-06 contratto di scambio incompleto | Versione, identità campo, stato dei tipi e CRS nativo attraversano il bordo IO senza perdita silenziosa | `plenora.contract.version`; `plenora.field_id`; `types_declaration`; cinque chiavi CRS; parser fail-closed; golden test model; harness reale IO → data → database su Point XYZ | Parziale: una sola direzione e una sola fixture della matrice di sistema |
| PLN-ASR-016 | H-02/H-03 operazione lunga non interrompibile | Ogni operazione espone cancellazione e deadline ai confini controllati e non pubblica dopo cancellazione | `CancellationToken` in opzioni/richieste; controlli probe/batch/write/finalize; KML event-based, DXF progressivo e XLSX lazy ricontrollano durante le rispettive scansioni; test rilascio lease, intervallo bounded e pre-publish; benchmark pre/post | Parziale: la singola chiamata interna delle dipendenze non è preemptive |
| PLN-ASR-017 | H-01/H-09 errore ambiguo | Categoria, fase, effetto remoto e retry sono indipendenti e serializzabili | `PlenoraIoError`; busta CLI con assi `snake_case`; retry taggato con `delay_ms` per `after`; golden test delle cinque varianti e test CLI; messaggio separato dagli assi | Soddisfatto nel componente; convergenza cross-repo da verificare |
| PLN-ASR-018 | H-07/H-09 dichiarazione di release ambigua | Una RC identifica ICD, revisione, stato normativo, deroghe e perimetro componente/sistema senza promozioni implicite | Candidato 1.0 RC1 `796a1f9`, decisione `409cf93`, pre-tag `cea2535`, record/tag target `6e3a942`; CI del tag `30619802027` verde | Soddisfatto per 1.0 RC1 |
| PLN-ASR-019 | H-01/H-08 codec divergenti non rilevati | WKB/EWKB usa corpus e invarianti condivisi e classifica ogni divergenza fra i due bordi | Replay incrociato di 18 casi: pass, zero divergenze non classificate e due differenze motivate; campagna lunga riproducibile su harness committati: 210.118.046 esecuzioni libFuzzer e 5.705.840.000 iterazioni strutturate, zero finding; corpus rigenerabile verificato in CI | Parziale: retention dei finding e promozione del corpus nel repository condiviso ancora aperte |
| PLN-ASR-020 | H-01/H-07 risultato non riproducibile | Ogni operazione dichiara il livello di determinismo e il catalogo non dipende dall'ordine di registrazione | `DeterminismLevel`; catalogo ordinato; doppio round-trip pure-Rust; doppia scrittura FileGDB confrontata semanticamente; matrice nativa Windows fissata per digest con GDAL 3.10.3/OpenFileGDB e benchmark narrow | Parziale: matrice multi-GDAL e qualifica di ulteriori filesystem restano aperte |
| PLN-ASR-021 | H-01/H-06/H-08 proprietà perse nella catena | Il contratto deve attraversare componenti reali, non soli test isolati | IO-tools emette anche `ARROW:extension:name` insieme alle chiavi canoniche; `convert` rende machine-readable il `LossReport` reader; ownership della qualifica dichiarata in `release/system-rc-gate.json` | Aperto esterno: il runner deve consumare `read_loss` e la matrice deve essere rieseguita fuori da questo repository sulla baseline candidata |
| PLN-ASR-022 | H-01/H-06 scelta CRS non autorizzata | Un writer deve dichiarare se ogni rappresentazione CRS è preservata, assente o derivata e non può conciliare una discordanza senza autorità | `CrsRepresentationCapabilities`; catalogo dei dieci driver; preflight `CrsRepresentationsInconsistent`; test IPC-pass/Shapefile-fail e riapertura IPC con `crs_id`/`srid` invariati; categorie writer loss per rappresentazione e stato | Parziale: implementazione candidata R4.6.5, ratifica owner aperta; confronto semantico della definizione fuori scope |
| PLN-ASR-023 | H-06/H-08 proprietà canoniche non comprese | Conservazione opaca e comprensione semantica devono essere misurate separatamente | R2.8 e R4.1.1 registrate in `release/rc5-development.json`; nessuna modifica anticipata a `is_geometry_field` o `RawCrs`; matrice esterna duplicata canonica/GeoArrow | Aperto per le due proposte: baseline esterna pre-intervento IO-tools 14/14 canonica e 13/14 GeoArrow, da rieseguire sulla revisione candidata |
| PLN-ASR-024 | H-07/H-09 superficie 1.x ambigua | La compatibilità 1.x deve indicare una superficie concreta e verificabile | `release/cli-protocol-v1.json` congela sei buste JSON; gate negativi vietano rimozione del contratto `convert`; crate Rust `publish=false` e API dichiarata interna | Soddisfatto per il candidato 1.0 RC1 |
| PLN-ASR-025 | H-01/H-08 qualifica post-tag non attribuibile | Le prove di catena devono usare riferimenti immutabili e distinguere gap proposti da violazioni ratificate | `plenora-contracts@c3f1a8e`; tre tag immutabili; roundtrip `83/84`, catena `27/28`; singolo fallimento `crs_unresolved__geoarrow` attribuito a R4.1.1 proposta; osservazione mirata `read_loss` sul conflitto GeoArrow | Parziale esterno: direzione inversa, Windows e consumo automatico di `read_loss` aperti |

## Hazard

| ID | Descrizione |
|---|---|
| H-01 | Corruzione, perdita o reinterpretazione silenziosa dei dati |
| H-02 | Sovrascrittura o pubblicazione parziale di un dataset |
| H-03 | Esaurimento non controllato di memoria, CPU o storage |
| H-04 | Panic, comportamento indefinito o arresto del processo |
| H-05 | Dichiarazione di durabilità non realmente confermata |
| H-06 | CRS o ordine assi interpretato in modo errato |
| H-07 | Artefatto non riproducibile o dipendenza non controllata |
| H-08 | Difetto non rilevato dalla strategia di verifica |
| H-09 | Regressione introdotta senza change impact analysis |
